spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true
      path: /h2-console
security:
  secret: s3cr#t@@159753@@!!456456@##!s3cr#t
  public-paths:
    - /v1/authenticate